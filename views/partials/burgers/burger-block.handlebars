<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdinner="{{devoured}}">
		{{#if devoured}}ENJOY BURGER!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{id}}">DELETE!</button>
</li>
